# Core
Django
djangorestframework
django-cors-headers
python-dotenv

# Background jobs
celery
redis

# CV / video
PyYAML==6.0.1
opencv-python-headless==4.10.0.84
python-dotenv~=1.0.0
numpy==2.2.6
moviepy==1.0.3
ffmpeg-python
pandas

# Deep learning (CPU default wheel).
# If you need CUDA, see note below to install from the PyTorch index.
torch
torchvision

# Jupyter kernel for this venv
ipykernel

# Inference toolkit with extras
# (transformers, SAM, CLIP, gaze, GroundingDINO)
inference[transformers,sam,clip,gaze,grounding-dino]

# Football Analytics
ultralytics
matplotlib
tqdm
timm
transformers
sentencepiece
gdown
supervision
more_itertools
scikit-learn
umap-learn
